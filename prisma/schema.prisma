generator client { provider = "prisma-client-js" }
datasource db { provider = "postgresql"; url = env("DATABASE_URL") }

// TODO: Defina o model Produto com campos: id, nome, preco
// model Produto {
//   id    Int    @id @default(autoincrement())
//   nome  String
//   preco Float
// }
